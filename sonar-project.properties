# required metadata 
# unique project identifier (required)
sonar.projectKey=KQ:CodeKatabySonarRunner
sonar.projectName=CodeKatabySonarRunner
sonar.projectVersion=2.0.0
  
sonar.login=65ab072a0309773c89ccfd37dd19ed219424d7c0
  
# path to source directories (required) 
sonar.sources=CodeKata-basic\src\main\java
  
# path to test source directories (optional) 
#sonar.tests=src/test/java 
sonar.tests=CodeKata-basic\src\test\java
# path to project binaries (optional), for example directory of Java bytecode 
sonar.binaries=target/classes
  
# optional comma-separated list of paths to libraries. Only path to JAR file and path to directory of #classes are supported. 
#libraries=path/to/library.jar,path/to/classes/dir 
#libraries=junit.jar
#libraries=junit.jar 

  
# Uncomment those lines if some features of java 5 or java 6 like annotations, enum, ...  
# are used in the source code to be analysed 
#sonar.java.source=1.5
#sonar.java.target=1.5
  
  
# Uncomment this line to analyse a project which is not a java project.  
# The value of the property must be the key of the language. 
#sonar.language=cobol 
  
#Advanced parameters 
#my.property=value

# Import tests execution reports (JUnit XML format).
# Set the property to the path of the directory containing all the XML reports.
#sonar.junit.reportsPath=target/surefire-reports
 
# Import JaCoCo code coverage report.
# Set the property to the path of the JaCoCo .exec report.
#sonar.jacoco.reportPath=target/jacoco.exec
 
# Import Cobertura code coverage report.
# Set the property to the path of the Cobertura .ser report.
#sonar.cobertura.reportPath=target/site/cobertura/coverage.xml
 
# Import Clover code coverage report.
# Set the property to the path of the Clover .xml report.
#sonar.clover.reportPath=target/clover.xml